import { observer } from "mobx-react-lite";
import React, { useContext } from "react";
const UnitInformation : React.FC = () => {
    

    return (
        <div><h2>اطلاعات مجموعه</h2></div>
    );
};

export default observer( UnitInformation );
