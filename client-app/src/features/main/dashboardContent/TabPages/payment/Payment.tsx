import { observer } from "mobx-react-lite";
import React, { useContext } from "react";

const Payment: React.FC = () => {

    return (
        <div style={{height:"1500px"}}><h2>اطلاعات پرداخت</h2></div>
    );
};

export default observer(Payment);
