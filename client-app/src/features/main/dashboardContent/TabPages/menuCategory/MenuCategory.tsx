import { observer } from "mobx-react-lite";
import React, { useContext } from "react";

const MenuCategory: React.FC = () => {

    return (
        <div><h2>دسته بندی منو</h2></div>
    );
};

export default observer(MenuCategory);
