import { observer } from "mobx-react-lite";
import React, { useContext } from "react";

const CategoryItem: React.FC = () => {

    return (
        <div><h2>اطلاعات هر آیتم</h2></div>
    );
};

export default observer(CategoryItem);


