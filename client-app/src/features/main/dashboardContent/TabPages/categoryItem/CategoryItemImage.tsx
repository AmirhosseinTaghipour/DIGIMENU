import { observer } from 'mobx-react-lite';
import React from 'react'

const CategoryItemImage: React.FC = () => {
    return (
        <div>Category Item Images</div>
    )
}

export default observer(CategoryItemImage);

