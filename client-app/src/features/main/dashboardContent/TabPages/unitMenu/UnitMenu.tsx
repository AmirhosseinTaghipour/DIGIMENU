import { observer } from "mobx-react-lite";
import React, { useContext } from "react";

const UnitMenu: React.FC = () => {

    return (
        <div><h2>ایجاد منو</h2></div>
    );
};

export default observer(UnitMenu);
